@{
    var homeUrl = Href("~/");
}
<h1>
    <a href="@homeUrl">@WorkContext.CurrentSite.SiteName</a>
    @if (Layout.Title != null && !string.IsNullOrEmpty(Layout.Title))
    {
        <small>| @Layout.Title</small>
    }
</h1>
